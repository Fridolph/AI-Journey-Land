{
  "name": "@ai-journey-land/demo-registry",
  "version": "0.1.0",
  "private": true,
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch --preserveWatchOutput",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node -e \"console.log('demo-registry: no tests yet')\""
  },
  "dependencies": {
    "@ai-journey-land/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
